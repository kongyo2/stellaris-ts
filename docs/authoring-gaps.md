# What the authoring model cannot express

Generated by `npm run verify:reproduce`. Do not edit by hand.

The conformance report asks whether the schema knows what vanilla ships. This asks the harder
question: **could a mod author have written it?** Every vanilla definition is converted back into
what `define({ ... })` would have taken, printed, re-parsed, and compared with the original.

Nothing here is inferred from how a construct looks. A value list and a script-variable reference
both look exotic and are perfectly writable; a comparison operator looks ordinary and is not.
These are the ones that actually failed the round trip.

## Summary

| | |
| --- | --- |
| definitions inspected | 40550 |
| reproduced exactly | 39645 (97.77%) |
| blocked | 905 |
| distinct causes | 6 |
| files read | 2507 |

## Causes

| cause | definitions | files | example |
| --- | --- | --- | --- |
| `print-mismatch` | 477 | 126 | tb_others_group in `interface/resource_groups/topbar_other_resource_groups.txt` |
| `anonymous-block` | 137 | 30 | preset_vassal in `common/agreement_presets/00_agreement_presets.txt` |
| `prefixed-block` | 121 | 38 | country in `common/colors/00_colors.txt` |
| `duplicate-key-mixed-with-list` | 111 | 27 | cost in `common/inline_scripts/buildings/nomadic_cost_switcher_with_additional_trigger.txt` |
| `inline-math` | 38 | 19 | strike_craft_component_template in `common/component_templates/00_shroud_weapons.txt` |
| `optional-block` | 21 | 13 | pop_group_transfer_ethic in `common/scripted_effects/00_scripted_effects.txt` |

