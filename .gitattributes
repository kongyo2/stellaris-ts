# LF everywhere. `text=auto` alone would check out CRLF on Windows and break
# `prettier --check` (endOfLine: "lf") on a fresh clone.
* text=auto eol=lf

# Verbatim extracts of vanilla Stellaris files. Never normalise these — the
# round-trip test suite assumes byte-identical input.
tests/fixtures/** -text
