# Basic example

One building, one technology, and the strings for both.

```bash
npm run index:game            # once, so filename collisions can be checked
npx tsx examples/basic/mod.ts # writes into the game's mod folder
```

Then enable **stellaris-ts Example** in the launcher.

Set `STELLARIS_MODS_DIR` to write somewhere else.
