// Generated by `npm run propose:commands` from the installed game, then reviewed.
//
// Triggers and effects vanilla writes that cwtools-stellaris-config never
// declared and the newest -debug dump (4.3.7) predates. The game evaluates
// every one of these lines, so they exist; what they hold is only as narrow as
// the shapes vanilla was seen to write, and a block is left open rather than
// invented.
//
// Committed rather than recomputed: a command added by a later patch has to
// arrive as a diff.

import { anyValue, primitive, scriptCommand, unspecifiedScope } from "./ir.js";

import type { ScriptCommandDefinition } from "./ir.js";

export const vanillaCommands: readonly ScriptCommandDefinition[] = [
  // 1 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "cancel_contract",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 920 uses across 10 types, e.g. common/anomalies/95_anomaly_categories_distant_stars.txt
  scriptCommand({
    id: "carrier_event",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 17 uses across 1 type, e.g. events/aquatics_events.txt
  scriptCommand({
    id: "colony_event",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 12 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "create_balanced_fleet",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 4 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "every_issued_mission",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 37 uses across 2 types, e.g. events/assimilation_events.txt
  scriptCommand({
    id: "every_owned_colony",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 2 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "every_owned_contract",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 29 uses across 2 types, e.g. events/origin_events_astral_planes.txt
  scriptCommand({
    id: "every_system_planet_colony",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 12 uses across 2 types, e.g. events/shroud_events.txt
  scriptCommand({
    id: "every_system_ship_colony",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 6 uses across 2 types, e.g. events/origin_events_paragon.txt
  scriptCommand({
    id: "force_remove_civic_by_index",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
  // 1 uses across 1 type, e.g. events/marauder_events.txt
  scriptCommand({
    id: "force_show_diplomacy",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 26 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "issue_contract",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 2 uses across 1 type, e.g. events/ambition_events.txt
  scriptCommand({
    id: "lock_animation_state",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 30 uses across 4 types, e.g. common/missions/missions/02_contracts.txt
  scriptCommand({
    id: "random_owned_colony",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 4 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "random_owned_contract",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. events/nomads_arkship_events.txt
  scriptCommand({
    id: "random_owned_mission",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. events/shroud_events.txt
  scriptCommand({
    id: "random_system_planet_colony",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. events/shroud_events.txt
  scriptCommand({
    id: "random_system_ship_colony",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 335 uses across 9 types, e.g. common/buildings/15_overlord_holdings.txt
  scriptCommand({
    id: "remove_carrier_flag",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 1 uses across 1 type, e.g. events/nomads_arkship_events.txt
  scriptCommand({
    id: "return_from_mia",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 1 uses across 1 type, e.g. events/ambition_events.txt
  scriptCommand({
    id: "set_animation_state",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 977 uses across 10 types, e.g. common/anomalies/102_anomaly_categories_cosmic_storms.txt
  scriptCommand({
    id: "set_carrier_flag",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 2 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "set_faction_properties",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 2 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "set_resource_converter",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 2 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "set_system_locked",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 126 uses across 5 types, e.g. common/buildings/15_overlord_holdings.txt
  scriptCommand({
    id: "set_timed_carrier_flag",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 2 uses across 1 type, e.g. common/scripted_effects/nomads_effects.txt
  scriptCommand({
    id: "spawn_random_anomaly",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "steal_planet_output",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 3 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "transfer_resource_stockpile",
    family: "effect",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. common/scripted_triggers/00_scripted_triggers_ai.txt
  scriptCommand({
    id: "ai_armor_ratio",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("number"),
  }),
  // 1 uses across 1 type, e.g. common/scripted_triggers/00_scripted_triggers_ai.txt
  scriptCommand({
    id: "ai_shields_ratio",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("number"),
  }),
  // 3 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "any_issued_mission",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 66 uses across 12 types, e.g. common/ai_budget/00_alloys_budget.txt
  scriptCommand({
    id: "any_owned_colony",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 4 uses across 2 types, e.g. events/nomads_arkship_events.txt
  scriptCommand({
    id: "any_owned_contract",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 5 uses across 1 type, e.g. common/country_focus/focus_cards/00_core_focus_cards.txt
  scriptCommand({
    id: "any_owned_mission",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 3 uses across 1 type, e.g. common/megastructures/05_gateways.txt
  scriptCommand({
    id: "any_starbase_in_network",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 57 uses across 10 types, e.g. common/missions/missions/02_contracts.txt
  scriptCommand({
    id: "any_system_planet_colony",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 13 uses across 3 types, e.g. common/patrons/patron_types.txt
  scriptCommand({
    id: "any_system_ship_colony",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 22 uses across 4 types, e.g. common/colony_types/00_colony_types.txt
  scriptCommand({
    id: "carrier_is_type",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 1 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "count_available_contracts",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
  // 5 uses across 1 type, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "count_issued_contracts",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
  // 10 uses across 2 types, e.g. events/ancient_relics_arcsite_events_2.txt
  scriptCommand({
    id: "count_owned_colony",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. common/achievements.txt
  scriptCommand({
    id: "count_starbase_in_network",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 4 uses across 4 types, e.g. common/megastructures/14_hyper_relay.txt
  scriptCommand({
    id: "count_system_planet_colony",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. common/scripted_triggers/08_scripted_triggers_shroud.txt
  scriptCommand({
    id: "count_system_ship_colony",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 6 uses across 1 type, e.g. common/starbase_modules/00_arkship_spinal_modules.txt
  scriptCommand({
    id: "galaxy_radius",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
  // 2273 uses across 28 types, e.g. common/achievements.txt
  scriptCommand({
    id: "has_carrier_flag",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 6 uses across 3 types, e.g. events/nomads_arkship_events.txt
  scriptCommand({
    id: "has_contract",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 5 uses across 2 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "has_contract_of_category",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 9 uses across 4 types, e.g. common/casus_belli/07_nomads_casus_belli.txt
  scriptCommand({
    id: "has_waystation_pact",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 1678 uses across 64 types, e.g. common/achievements.txt
  scriptCommand({
    id: "is_nomadic",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 4 uses across 4 types, e.g. events/nomads_events_1.txt
  scriptCommand({
    id: "is_system_locked",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 33 uses across 10 types, e.g. common/colony_automation_exceptions/00_crisis_exceptions.txt
  scriptCommand({
    id: "is_unemployed",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 13 uses across 2 types, e.g. common/ai_budget/00_biomass_budget.txt
  scriptCommand({
    id: "num_owned_colonies",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
  // 1 uses across 1 type, e.g. common/policies/05_policies_nomads.txt
  scriptCommand({
    id: "num_waystation_pacts",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
  // 8 uses across 1 type, e.g. common/scripted_triggers/00_scripted_triggers.txt
  scriptCommand({
    id: "resource_stockpile_percent",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: anyValue(),
  }),
  // 1 uses across 1 type, e.g. common/situations/14_nomads_situations.txt
  scriptCommand({
    id: "situation_progress_percent",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("scalar"),
  }),
  // 2 uses across 2 types, e.g. common/achievements.txt
  scriptCommand({
    id: "starbase_network_size",
    family: "trigger",
    input: unspecifiedScope(),
    operator: "=",
    value: primitive("integer"),
  }),
];
