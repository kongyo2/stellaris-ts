// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId } from "../catalog.js";

import { block, defineType, effectEntries, enumRef, field, keyedBlocks, occurs, primitive } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const focusRewards: DefinitionType = defineType({
  id: DefinitionTypeId.FocusRewards,
  source: keyedBlocks("common/country_focus/focus_rewards"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("category", enumRef(EnumId.CardCategoriesFocus), occurs.optional),
    field("cost", primitive("integer"), occurs.one),
    field("mode", enumRef(EnumId.FocusRewardsMode), occurs.one),
    field("on_success", block([effectEntries()]), occurs.optional),
    field("category", enumRef(EnumId.CardCategoriesFocus), occurs.optional),
    field("cost", primitive("integer"), occurs.one),
    field("mode", enumRef(EnumId.FocusRewardsMode), occurs.one),
    field("on_success", block([effectEntries()]), occurs.optional),
  ],
});
