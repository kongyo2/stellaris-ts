// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  between,
  block,
  defineType,
  definitionLocalisation,
  enumRef,
  field,
  fieldEquals,
  fieldPresent,
  item,
  keyedBlocks,
  literal,
  modifierEntries,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  ruleSetEntries,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const councilor: DefinitionType = defineType({
  id: DefinitionTypeId.Councilor,
  source: keyedBlocks("common/governments/councilors"),
  variants: [
    { id: "unbound", when: fieldEquals("civic", "none") },
    { id: "civic", when: fieldPresent("civic") },
  ],
  localisation: [
    definitionLocalisation("Title", "$", true),
    definitionLocalisation("Title (Female)", "$_female", false),
    definitionLocalisation("Description", "$_desc", true),
  ],
  modifiers: [],
  entries: [
    field("civic", literal("none"), occurs.optional, {
      documentation: "Civic that is required for this council position. Also used by UI. (Default = none)",
    }),
    field("civic", typeRef("civic_or_origin", "civic"), occurs.optional, {
      documentation: "Civic that is required for this council position. Also used by UI. (Default = none)",
    }),
    field("leader_class", literal("none"), occurs.optional, {
      documentation:
        "Which leader classes can have this council position. Currently only used for the Recruit New Leader buttons. (Default = none)",
    }),
    field("leader_class", block([item(enumRef(EnumId.LeaderClasses), between(1, 5))]), occurs.optional, {
      documentation:
        "Which leader classes can have this council position. Currently only used for the Recruit New Leader buttons. (Default = none)",
    }),
    field("possible", block([triggerEntries()]), occurs.optional, {
      documentation: "Country scope triggers to evaluate if the council position is allowed.",
      scope: replaceScope({ root: ScopeId.Country }),
    }),
    field("is_leader_possible", block([triggerEntries()]), occurs.optional, {
      documentation: "Leader scope triggers to evaluate if a leader can be assigned to this position.",
      scope: replaceScope({ root: ScopeId.Leader }),
    }),
    field("modifier", block([modifierEntries()]), occurs.optional, {
      documentation: "Country modifier, that is multiplied by assigned Leader's level.",
      scope: replaceScope({ root: ScopeId.Country }),
    }),
    field("custom_tooltip", primitive("localisation"), occurs.optional, {
      documentation:
        "Custom tooltip text shown instead of the auto-generated modifier tooltip text of the council position.",
    }),
    field("custom_tooltip_with_modifiers", primitive("localisation"), occurs.optional, {
      documentation:
        "Custom tooltip text shown in addition to the auto-generated modifier tooltip text of the council position.",
    }),
    field("custom_unlock_title", primitive("localisation"), occurs.optional, {
      documentation:
        'Custom text shown instead of "UNLOCK_COUNCIL_POSITION_TOOLTIP" for council positions without a civic.',
    }),
    field("conditional_tooltip", primitive("localisation"), occurs.optional, {
      documentation: "Custom tooltip text shown for the requirements of the council position.",
    }),
    field("icon", typeRef("sprite"), occurs.optional, {
      documentation:
        "Icon used for the council position, if a civic is not specified. (Default = icon for the corresponding civic)",
    }),
    field("required", primitive("boolean"), occurs.optional, {
      documentation: "Is this position forced to be on the council. (Default = no)",
    }),
    field(
      "ai_hiring_weight",
      block([field("base", primitive("integer"), occurs.optional), modifierRuleEntries()]),
      occurs.optional,
    ),
    field("ai_priority", primitive("integer"), occurs.optional),
    field("optional", block([triggerEntries()]), occurs.optional),
    ruleSetEntries("triggered_country_modifier_country"),
  ],
});
