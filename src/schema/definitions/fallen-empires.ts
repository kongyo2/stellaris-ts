// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  effectEntries,
  field,
  keyedBlocks,
  literal,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const fallenEmpires: DefinitionType = defineType({
  id: DefinitionTypeId.FallenEmpires,
  source: keyedBlocks("common/fallen_empires"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("graphical_culture", typeRef("graphical_culture"), occurs.one),
    field("initializer", typeRef("solar_system_initializer", "fallen_empire_initializer_sub"), occurs.optional),
    field("initializer", literal("fallen_hive_last_thought"), occurs.optional),
    field("allow_custom_empire_design", primitive("boolean"), occurs.optional),
    field("possible", block([triggerEntries()]), occurs.optional, { scope: replaceScope({ root: ScopeId.Country }) }),
    field("weight_modifier", block([modifierRuleEntries()]), occurs.one, {
      scope: replaceScope({ root: ScopeId.Country }),
    }),
    field("create_country_effect", block([effectEntries()]), occurs.one, {
      scope: replaceScope({ root: ScopeId.Country }),
    }),
  ],
});
