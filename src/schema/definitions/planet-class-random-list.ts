// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { block, defineType, field, item, occurs, primitive, taggedBlocks, typeRef } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const planetClassRandomList: DefinitionType = defineType({
  id: DefinitionTypeId.PlanetClassRandomList,
  source: taggedBlocks("common/planet_classes", "name", [], true, {
    rootKeyFilter: { mode: "include", values: ["random_list"] },
  }),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("name", primitive("scalar"), occurs.one),
    field("planets", block([item(typeRef("planet_class"), occurs.oneOrMore)]), occurs.one),
    field("name", primitive("scalar"), occurs.one),
    field("planets", block([item(typeRef("planet_class"), occurs.oneOrMore)]), occurs.one),
  ],
});
