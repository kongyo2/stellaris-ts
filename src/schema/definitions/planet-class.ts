// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  allOf,
  between,
  block,
  defineType,
  definitionLocalisation,
  enumRef,
  field,
  fieldEquals,
  item,
  keyedBlocks,
  literal,
  modifierEntries,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
  typeRef,
  whenVariant,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const planetClass: DefinitionType = defineType({
  id: DefinitionTypeId.PlanetClass,
  source: keyedBlocks("common/planet_classes", true, { rootKeyFilter: { mode: "exclude", values: ["random_list"] } }),
  variants: [
    { id: "star", when: fieldEquals("star", true) },
    { id: "planet", when: fieldEquals("star", false) },
    { id: "habitable_planet", when: fieldEquals("colonizable", true) },
    { id: "unhabitable_ringworld", when: allOf(fieldEquals("ringworld", true), fieldEquals("colonizable", false)) },
  ],
  localisation: [
    definitionLocalisation("Name", "$", true),
    definitionLocalisation("Description", "$_desc", true),
    definitionLocalisation("Habitability", "mod_$_habitability", true, "habitable_planet"),
    definitionLocalisation("Preference trait", "trait_$_preference", true, "habitable_planet"),
    definitionLocalisation("Preference trait description", "trait_$_preference_desc", true, "habitable_planet"),
  ],
  modifiers: [],
  entries: [
    field("atmosphere_color", primitive("colour", undefined, undefined, "hsv"), occurs.optional),
    field("atmosphere_intensity", primitive("number"), occurs.optional),
    field("atmosphere_width", primitive("number"), occurs.optional),
    field("entity", primitive("scalar"), occurs.one),
    field("synaptic_lathe", primitive("boolean"), occurs.optional),
    field("preview_entity", primitive("scalar"), occurs.optional),
    field("overlay_entity", primitive("scalar"), occurs.optional),
    field("entity_scale", primitive("number"), occurs.optional),
    field("outliner_planet_type", literal("special"), occurs.optional),
    field("fixed_entity_scale", primitive("boolean"), occurs.optional),
    field("icon", typeRef("sprite"), occurs.optional),
    field("icon_large", typeRef("sprite"), occurs.one),
    whenVariant("planet", [
      field("icon", typeRef("sprite"), occurs.one),
      field("habitat", primitive("boolean"), occurs.optional),
      field("outliner_planet_type", literal("special"), occurs.optional),
    ]),
    whenVariant("star", [field("star_gfx", primitive("boolean"), occurs.optional)]),
    field("spawn_odds", primitive("number"), occurs.optional),
    field("production_spawn_chance", primitive("number"), occurs.optional),
    field("city_color_lut", primitive("scalar"), occurs.optional),
    field("extra_orbit_size", primitive("number"), occurs.optional),
    field("extra_planet_count", primitive("number"), occurs.optional),
    field("chance_of_ring", primitive("number"), occurs.optional),
    field("min_distance_from_sun", primitive("integer"), occurs.optional),
    field("max_distance_from_sun", primitive("integer"), occurs.optional),
    field(
      "planet_size",
      block([field("min", primitive("integer"), occurs.one), field("max", primitive("integer"), occurs.one)]),
      occurs.optional,
    ),
    field("planet_size", primitive("integer"), occurs.optional),
    field("moon_size", primitive("integer"), occurs.optional),
    field(
      "moon_size",
      block([field("min", primitive("integer"), occurs.one), field("max", primitive("integer"), occurs.one)]),
      occurs.optional,
    ),
    field("enable_tilt", primitive("boolean"), occurs.optional),
    field("place_entity_on_planet_plane", primitive("boolean"), occurs.optional),
    field("entity_face_object", primitive("boolean"), occurs.optional),
    field("can_be_moon", primitive("boolean"), occurs.optional),
    field("space_monster_target", primitive("boolean"), occurs.optional),
    field("asteroid", primitive("boolean"), occurs.optional),
    field("star", primitive("boolean"), occurs.optional),
    field("ringworld", primitive("boolean"), occurs.optional),
    field("can_have_no_pop_colony", primitive("boolean"), occurs.optional, { documentation: "(default: no)" }),
    field("can_be_invaded", primitive("boolean"), occurs.optional),
    field("orbit_lines", primitive("boolean"), occurs.optional),
    field("has_colonization_influence_cost", primitive("boolean"), occurs.optional, {
      documentation: "applies when within own borders only",
    }),
    field("survey_time_factor", primitive("number"), occurs.optional),
    field("colonization_tech", block([item(typeRef("technology"), occurs.one)]), occurs.optional),
    field("picture", primitive("scalar"), occurs.optional, {
      documentation: "this is only required if using a picture defined for another planet",
    }),
    field("tile_set", typeRef("planet_class"), occurs.optional, {
      documentation: "this is only required if using a tile-set defined for another planet",
    }),
    field("modifier", block([modifierEntries()]), occurs.optional, { scope: replaceScope({ root: ScopeId.Planet }) }),
    field("allowed_archetypes", block([item(typeRef("species_archetype"), between(0, 100))]), occurs.optional),
    field("uses_alternative_skies_for_moons", primitive("boolean"), occurs.optional, {
      documentation: "(default: no)",
    }),
    field("uses_alternative_skies_if_has_orbital_ring", primitive("boolean"), occurs.optional),
    field("fixed_city_level", primitive("integer"), occurs.optional),
    field("is_artificial_planet", primitive("boolean"), occurs.optional),
    field("colonizable", primitive("boolean"), occurs.optional),
    whenVariant("habitable_planet", [
      field("district_set", enumRef(EnumId.DistrictSets), occurs.one, { severity: "warning" }),
      field("climate", primitive("localisation"), occurs.optional),
      field("habitat", primitive("boolean"), occurs.optional),
      field("starting_planet", primitive("boolean"), occurs.optional),
      field("show_city", primitive("boolean"), occurs.optional),
      field("ideal", primitive("boolean"), occurs.optional),
      field("initial", literal(true), occurs.optional),
      field("potential", block([triggerEntries()]), occurs.optional),
      field("auto_trait_prio", block([item(typeRef("trait"), occurs.one)]), occurs.optional),
      field("starting_district", typeRef("district"), occurs.optional),
      field("carry_cap_per_free_district", primitive("number"), occurs.optional),
    ]),
    whenVariant("unhabitable_ringworld", [
      field("district_set", enumRef(EnumId.DistrictSets), occurs.optional, { severity: "warning" }),
    ]),
    field("colonizable_by_event", primitive("boolean"), occurs.optional),
    field("show_health_bar_in_planet_view", primitive("boolean"), occurs.optional),
    field("starting_planet", literal(false), occurs.optional),
    field("show_city", literal(false), occurs.optional),
    field("default_planet_selection", primitive("boolean"), occurs.optional),
    field("inherit_country_building_modifiers", primitive("boolean"), occurs.optional),
    field("planet_custom_label_offset", primitive("number"), occurs.optional),
    field("astral_rift_gfx", literal(true), occurs.optional),
    field("astral_scar", literal(true), occurs.optional),
    field("ideal", literal(true), occurs.optional),
  ],
});
