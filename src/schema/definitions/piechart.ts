// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { between, block, defineType, field, item, occurs, primitive, taggedBlocks } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const piechart: DefinitionType = defineType({
  id: DefinitionTypeId.Piechart,
  source: taggedBlocks("interface", "name", [], true, {
    rootKeyFilter: { mode: "include", values: ["PieChartType"] },
    container: { kind: "named-container", key: "spriteTypes" },
  }),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("name", primitive("scalar"), occurs.one),
    field("size", primitive("integer"), occurs.one),
    field("is_hover", primitive("boolean"), occurs.optional),
    field(
      "colors",
      block([field("rgb", block([item(primitive("integer"), between(3, 3))]), occurs.any)]),
      occurs.optional,
    ),
    field("name", primitive("scalar"), occurs.one),
    field("size", primitive("integer"), occurs.one),
    field("is_hover", primitive("boolean"), occurs.optional),
    field(
      "colors",
      block([field("rgb", block([item(primitive("integer"), between(3, 3))]), occurs.any)]),
      occurs.optional,
    ),
  ],
});
