// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import {
  block,
  defineType,
  field,
  fieldEquals,
  keyedBlocks,
  modifierRuleEntries,
  occurs,
  primitive,
  triggerEntries,
  typeRef,
  unlessVariant,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const planetModifier: DefinitionType = defineType({
  id: DefinitionTypeId.PlanetModifier,
  source: keyedBlocks("common/planet_modifiers"),
  variants: [{ id: "null", when: fieldEquals("is_null", true) }],
  localisation: [],
  modifiers: [],
  entries: [
    field("is_null", primitive("boolean"), occurs.optional),
    field("potential", block([triggerEntries()]), occurs.optional),
    field("spawn_chance", block([modifierRuleEntries()]), occurs.one),
    unlessVariant("null", [field("modifier", typeRef("static_modifier"), occurs.one)]),
    field("is_null", primitive("boolean"), occurs.optional),
    field("potential", block([triggerEntries()]), occurs.optional),
    field("spawn_chance", block([modifierRuleEntries()]), occurs.one),
    unlessVariant("null", [field("modifier", typeRef("static_modifier"), occurs.one)]),
  ],
});
