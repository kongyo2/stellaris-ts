// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  definitionLocalisation,
  field,
  generatedModifier,
  keyedBlocks,
  modifierEntries,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  ruleSetEntries,
  triggerEntries,
  typeKey,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const socialStrata: DefinitionType = defineType({
  id: DefinitionTypeId.SocialStrata,
  source: keyedBlocks("common/pop_categories"),
  entryScope: ScopeId.PopGroup,
  variants: [],
  localisation: [
    definitionLocalisation("Name", "pop_cat_$", true),
    definitionLocalisation("Plural", "pop_cat_$_plural", true),
    definitionLocalisation("Desc", "pop_cat_$_desc", true),
  ],
  modifiers: [
    generatedModifier("pop_cat_", "_happiness", "Pops"),
    generatedModifier("pop_cat_", "_political_power", "Pops"),
  ],
  entries: [
    field("rank", primitive("integer"), occurs.one),
    field("can_change_category", block([triggerEntries()]), occurs.optional),
    field("can_change_category", primitive("boolean"), occurs.optional),
    field("clothes_texture_index", primitive("integer"), occurs.optional),
    field("assign_to_pop", block([triggerEntries()]), occurs.optional, {
      documentation: "This is called whenever a Pop is created, moved to a different planet, or gets a new owner",
    }),
    field("weight", block([modifierRuleEntries()]), occurs.optional),
    field(
      "automation_resources",
      // vanilla 4.4.6: one of 24 strata writes `resources` with no `category`.
      block([field("category", typeRef("economic_category"), occurs.optional), ruleSetEntries("economic_template")]),
      occurs.any,
    ),
    field("pop_group_modifier", block([modifierEntries()]), occurs.any, {
      scope: replaceScope({ root: ScopeId.PopGroup }),
    }),
    field(
      "triggered_pop_group_modifier",
      block([field("potential", block([triggerEntries()]), occurs.one), modifierEntries()]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.PopGroup }) },
    ),
    field("allow_resettlement", block([triggerEntries()]), occurs.one, {
      documentation: "scope: pop_group to be resettled",
    }),
    field("should_apply_unemployment_penalties", block([triggerEntries()]), occurs.optional),
    field("unemployment_penalties", block([modifierEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Planet }),
    }),
    field(
      "resettlement_cost",
      block([
        field(typeKey("resource"), primitive("integer"), occurs.any),
        field("trigger", block([triggerEntries()]), occurs.optional),
      ]),
      occurs.any,
    ),
    field(
      "unemployment_resources",
      // vanilla 4.4.6: one of 24 strata writes this block with no `category`.
      block([field("category", typeRef("economic_category"), occurs.optional), ruleSetEntries("economic_template")]),
      occurs.optional,
    ),
    field(
      "resources",
      // vanilla 4.4.6: one of 24 strata writes this block with no `category`.
      block([field("category", typeRef("economic_category"), occurs.optional), ruleSetEntries("economic_template")]),
      occurs.optional,
    ),
    field("keep_from_former_job", primitive("boolean"), occurs.optional),
    field("demotion_time", primitive("integer"), occurs.optional),
    field("change_job_threshold", primitive("number"), occurs.optional),
    field("display_category", typeRef("social_strata"), occurs.optional),
    field("display_unemployment", primitive("boolean"), occurs.optional),
    field("custom_demotion_loc", primitive("scalar"), occurs.optional),
    field("custom_demotion_icon", primitive("scalar"), occurs.optional),
    field("custom_promotion_loc", primitive("scalar"), occurs.optional),
    field("custom_promotion_icon", primitive("scalar"), occurs.optional),
    ruleSetEntries("triggered_planet_modifier_pop"),
    ruleSetEntries("triggered_pop_modifier"),
    ruleSetEntries("triggered_pop_group_modifier"),
  ],
});
