// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId } from "../catalog.js";

import {
  block,
  defineType,
  enumKey,
  enumRef,
  field,
  keyedBlocks,
  literal,
  modifierRuleEntries,
  occurs,
  primitive,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const aiBudget: DefinitionType = defineType({
  id: DefinitionTypeId.AiBudget,
  source: keyedBlocks("common/ai_budget"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("resource", typeRef("resource"), occurs.one),
    field("type", literal("expenditure"), occurs.one),
    field("type", literal("upkeep"), occurs.one),
    field("category", typeRef("economic_category"), occurs.one),
    field("potential", block([triggerEntries()]), occurs.one),
    field(
      "weight",
      block([field(enumKey(EnumId.WeightOrBase), primitive("number"), occurs.one), modifierRuleEntries()]),
      occurs.optional,
    ),
    field("desired_max", block([modifierRuleEntries()]), occurs.optional),
    field(
      "desired_min",
      block([
        modifierRuleEntries(),
        field("modifier", block([field("factor", enumRef(EnumId.ExeSetVariable), occurs.one)]), occurs.any),
      ]),
      occurs.optional,
    ),
  ],
});
