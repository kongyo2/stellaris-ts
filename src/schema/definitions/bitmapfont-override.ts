// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId } from "../catalog.js";

import { between, block, defineType, enumRef, field, item, keyedBlocks, occurs, primitive, typeRef } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const bitmapfontOverride: DefinitionType = defineType({
  id: DefinitionTypeId.BitmapfontOverride,
  source: keyedBlocks("interface", true, {
    rootKeyFilter: { mode: "include", values: ["bitmapfont_override"] },
    container: { kind: "named-container", key: "bitmapfonts" },
  }),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("name", typeRef("bitmapfont"), occurs.one),
    field("ttf_font", typeRef("font"), occurs.optional),
    field("ttf_size", primitive("integer"), occurs.optional),
    field("vertical_offset", primitive("integer"), occurs.optional),
    field(
      "fontfiles",
      block([item(primitive("scalar"), occurs.oneOrMore, { severity: "information" })]),
      occurs.optional,
    ),
    field("languages", block([item(enumRef(EnumId.FontLanguages), between(1, 3))]), occurs.one),
    field(
      "multiline",
      block([
        field("line_break", block([item(primitive("scalar"), occurs.any)]), occurs.optional),
        field("forbidden_start", block([item(primitive("scalar"), occurs.any)]), occurs.optional),
      ]),
      occurs.optional,
    ),
  ],
});
