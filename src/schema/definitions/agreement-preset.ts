// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  allOf,
  block,
  defineType,
  field,
  fieldEquals,
  fieldPresent,
  item,
  keyedBlocks,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
  typeRef,
  whenVariant,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const agreementPreset: DefinitionType = defineType({
  id: DefinitionTypeId.AgreementPreset,
  source: keyedBlocks("common/agreement_presets"),
  variants: [
    { id: "specialist_type", when: fieldPresent("specialist_type") },
    { id: "hidden", when: fieldEquals("hidden", true) },
    { id: "has_parent", when: allOf(fieldPresent("parent"), fieldEquals("hidden", true)) },
  ],
  localisation: [],
  modifiers: [],
  entries: [
    whenVariant("has_parent", [field("parent", typeRef("agreement_preset"), occurs.optional)]),
    field("icon", typeRef("sprite"), occurs.optional),
    field("specialist_type", typeRef("subject_specialist_type"), occurs.optional),
    field(
      "term_data",
      block([
        field("has_cooldown_on_first_renegotiation", primitive("boolean"), occurs.optional, {
          documentation: "default: yes",
        }),
        field("forced_initial_loyalty", primitive("integer"), occurs.optional),
        field(
          "discrete_terms",
          block([
            item(
              block([
                field("key", typeRef("agreement_term", "discrete"), occurs.one, {
                  documentation: "The term name, as defined in agreement_terms",
                }),
                field("key", typeRef("agreement_term", "discrete_number"), occurs.one),
                field("key", typeRef("agreement_term", "specialist_type"), occurs.one),
                field("value", typeRef("agreement_term_value", "discrete"), occurs.one, {
                  documentation: "The term value to use, as defined in agreement_term_values",
                }),
                field("value", typeRef("agreement_term_value", "discrete_number"), occurs.one),
                field("value", typeRef("agreement_term_value", "specialist_type"), occurs.one),
              ]),
              occurs.any,
            ),
          ]),
          occurs.optional,
          { documentation: "The default discrete terms of this preset" },
        ),
        field(
          "resource_terms",
          block([
            item(
              block([
                field("key", typeRef("agreement_term_value", "resource"), occurs.one, {
                  documentation: "The resource term value to use, as defined in agreement_term_values",
                }),
                field("value", primitive("number", { min: -1, max: 1 }), occurs.one, {
                  documentation: "subsidy percentage",
                }),
              ]),
              occurs.any,
            ),
          ]),
          occurs.optional,
        ),
      ]),
      occurs.optional,
    ),
    field("should_ai_use_for_proposals", primitive("boolean"), occurs.optional),
    field("can_preset_be_changed", primitive("boolean"), occurs.optional),
    field("overlord_weight", block([modifierRuleEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("subject_weight", block([modifierRuleEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("potential", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("hidden", primitive("boolean"), occurs.optional, {
      documentation: "Whether this preset will be hidden from UI but the AI will be able to use it (default: no)",
    }),
    field("should_ai_use_for_proposals", primitive("boolean"), occurs.optional, {
      documentation:
        "Whether ai will skip this when trying to find best/preferred agreement preset to negotiate existing or propose new agreement (default: no)",
    }),
    whenVariant("has_parent", [field("parent", typeRef("agreement_preset"), occurs.optional)]),
    field("icon", typeRef("sprite"), occurs.optional),
    field("specialist_type", typeRef("subject_specialist_type"), occurs.optional),
    field(
      "term_data",
      block([
        field("has_cooldown_on_first_renegotiation", primitive("boolean"), occurs.optional, {
          documentation: "default: yes",
        }),
        field("forced_initial_loyalty", primitive("integer"), occurs.optional),
        field(
          "discrete_terms",
          block([
            item(
              block([
                field("key", typeRef("agreement_term", "discrete"), occurs.one, {
                  documentation: "The term name, as defined in agreement_terms",
                }),
                field("key", typeRef("agreement_term", "discrete_number"), occurs.one),
                field("key", typeRef("agreement_term", "specialist_type"), occurs.one),
                field("value", typeRef("agreement_term_value", "discrete"), occurs.one, {
                  documentation: "The term value to use, as defined in agreement_term_values",
                }),
                field("value", typeRef("agreement_term_value", "discrete_number"), occurs.one),
                field("value", typeRef("agreement_term_value", "specialist_type"), occurs.one),
              ]),
              occurs.any,
            ),
          ]),
          occurs.optional,
          { documentation: "The default discrete terms of this preset" },
        ),
        field(
          "resource_terms",
          block([
            item(
              block([
                field("key", typeRef("agreement_term_value", "resource"), occurs.one, {
                  documentation: "The resource term value to use, as defined in agreement_term_values",
                }),
                field("value", primitive("number", { min: -1, max: 1 }), occurs.one, {
                  documentation: "subsidy percentage",
                }),
              ]),
              occurs.any,
            ),
          ]),
          occurs.optional,
        ),
      ]),
      occurs.optional,
    ),
    field("should_ai_use_for_proposals", primitive("boolean"), occurs.optional),
    field("can_preset_be_changed", primitive("boolean"), occurs.optional),
    field("overlord_weight", block([modifierRuleEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("subject_weight", block([modifierRuleEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("potential", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("hidden", primitive("boolean"), occurs.optional, {
      documentation: "Whether this preset will be hidden from UI but the AI will be able to use it (default: no)",
    }),
    field("should_ai_use_for_proposals", primitive("boolean"), occurs.optional, {
      documentation:
        "Whether ai will skip this when trying to find best/preferred agreement preset to negotiate existing or propose new agreement (default: no)",
    }),
  ],
});
