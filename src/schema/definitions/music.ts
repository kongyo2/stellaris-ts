// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { defineType, definitionLocalisation, field, occurs, primitive, taggedBlocks, typeRef } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const music: DefinitionType = defineType({
  id: DefinitionTypeId.Music,
  source: taggedBlocks("music", "name", [], true, { rootKeyFilter: { mode: "include", values: ["music"] } }),
  variants: [],
  localisation: [definitionLocalisation("name", "", false)],
  modifiers: [],
  entries: [
    field("name", typeRef("song"), occurs.one),
    field("file", primitive("scalar"), occurs.one),
    field("volume", primitive("number"), occurs.optional),
    field("name", typeRef("song"), occurs.one),
    field("file", primitive("scalar"), occurs.one),
    field("volume", primitive("number"), occurs.optional),
  ],
});
