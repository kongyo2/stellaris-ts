// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { defineType, field, fieldEquals, keyedBlocks, literal, occurs } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const depositCategory: DefinitionType = defineType({
  id: DefinitionTypeId.DepositCategory,
  source: keyedBlocks("common/deposit_categories"),
  variants: [{ id: "blocker", when: fieldEquals("blocker", true) }],
  localisation: [],
  modifiers: [],
  entries: [
    field("blocker", literal(true), occurs.optional),
    field("important", literal(true), occurs.optional),
    field("blocker", literal(true), occurs.optional),
    field("important", literal(true), occurs.optional),
  ],
});
