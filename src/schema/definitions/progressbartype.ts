// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { between, block, defineType, field, item, occurs, primitive, taggedBlocks } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const progressbartype: DefinitionType = defineType({
  id: DefinitionTypeId.Progressbartype,
  source: taggedBlocks("interface", "name", [], true, {
    rootKeyFilter: { mode: "include", values: ["progressbartype"] },
    container: { kind: "named-container", key: "spriteTypes" },
  }),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("name", primitive("scalar"), occurs.one),
    field("textureFile", primitive("file"), occurs.optional),
    field("textureFile1", primitive("file"), occurs.optional),
    field("textureFile2", primitive("file"), occurs.optional),
    field("flipdirection", primitive("boolean"), occurs.optional),
    field("effectFile", primitive("file"), occurs.one),
    field("color", block([item(primitive("number"), between(3, 3))]), occurs.one),
    field("colortwo", block([item(primitive("number"), between(3, 3))]), occurs.one),
    field(
      "size",
      block([field("x", primitive("number"), occurs.one), field("y", primitive("number"), occurs.one)]),
      occurs.optional,
    ),
    field("horizontal", primitive("boolean"), occurs.optional),
  ],
});
