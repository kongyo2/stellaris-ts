// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { block, defineType, field, keyedBlocks, occurs, primitive, typeRef } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const mapGalaxyShape: DefinitionType = defineType({
  id: DefinitionTypeId.MapGalaxyShape,
  source: keyedBlocks("map/galaxy"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("core_radius_perc", primitive("number"), occurs.optional, {
      documentation: "Core radius is x% of the galaxy radius",
    }),
    field("num_stars_core_perc", primitive("number"), occurs.optional, { documentation: "Number of stars in core" }),
    field("stars_min_dist", primitive("number"), occurs.optional, { documentation: "Min distance between stars" }),
    field(
      "countries",
      block([
        field("ideal_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Ideal square distance between countries",
        }),
        field("min_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Min square distance between countries",
        }),
      ]),
      occurs.optional,
    ),
    field(
      "fallen_empires",
      block([
        field("ideal_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Ideal square distance between countries",
        }),
        field("min_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Min square distance between countries",
        }),
      ]),
      occurs.optional,
    ),
    field("preview_icon", typeRef("sprite"), occurs.optional),
    field("button_icon", typeRef("sprite"), occurs.optional),
    field("desc", primitive("localisation"), occurs.optional),
    field("num_arms", primitive("integer"), occurs.optional),
    field(
      "ring",
      block([
        field("width", primitive("number"), occurs.one, {
          documentation: "Percentage of radius to use as the width of the ring",
        }),
        field("offset", primitive("number"), occurs.one, {
          documentation: "Percentage of radius to use as the offset from center for the ring",
        }),
      ]),
      occurs.optional,
    ),
    field(
      "arms",
      block([
        field("tightness_winding", primitive("number"), occurs.one, { documentation: "Tightness of winding" }),
        field("width", primitive("number"), occurs.one, { documentation: "Arm width in degrees" }),
        field("fuzz", primitive("number"), occurs.one, { documentation: "Maximum outliers distance from arms" }),
        field("seperation", primitive("number"), occurs.one, {
          documentation: "Separation (in degrees) between each arm",
        }),
      ]),
      occurs.optional,
    ),
    field("core_radius_perc", primitive("number"), occurs.optional, {
      documentation: "Core radius is x% of the galaxy radius",
    }),
    field("num_stars_core_perc", primitive("number"), occurs.optional, { documentation: "Number of stars in core" }),
    field("stars_min_dist", primitive("number"), occurs.optional, { documentation: "Min distance between stars" }),
    field(
      "countries",
      block([
        field("ideal_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Ideal square distance between countries",
        }),
        field("min_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Min square distance between countries",
        }),
      ]),
      occurs.optional,
    ),
    field(
      "fallen_empires",
      block([
        field("ideal_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Ideal square distance between countries",
        }),
        field("min_sq_dist_between", primitive("integer"), occurs.one, {
          documentation: "int*int Min square distance between countries",
        }),
      ]),
      occurs.optional,
    ),
    field("preview_icon", typeRef("sprite"), occurs.optional),
    field("button_icon", typeRef("sprite"), occurs.optional),
    field("desc", primitive("localisation"), occurs.optional),
    field("num_arms", primitive("integer"), occurs.optional),
    field(
      "ring",
      block([
        field("width", primitive("number"), occurs.one, {
          documentation: "Percentage of radius to use as the width of the ring",
        }),
        field("offset", primitive("number"), occurs.one, {
          documentation: "Percentage of radius to use as the offset from center for the ring",
        }),
      ]),
      occurs.optional,
    ),
    field(
      "arms",
      block([
        field("tightness_winding", primitive("number"), occurs.one, { documentation: "Tightness of winding" }),
        field("width", primitive("number"), occurs.one, { documentation: "Arm width in degrees" }),
        field("fuzz", primitive("number"), occurs.one, { documentation: "Maximum outliers distance from arms" }),
        field("seperation", primitive("number"), occurs.one, {
          documentation: "Separation (in degrees) between each arm",
        }),
      ]),
      occurs.optional,
    ),
  ],
});
