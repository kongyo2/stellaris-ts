// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { block, defineType, field, item, keyedBlocks, literal, occurs, primitive } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const timelineEventsType: DefinitionType = defineType({
  id: DefinitionTypeId.TimelineEventsType,
  source: keyedBlocks("common/timeline_events"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("fire_only_once", primitive("boolean"), occurs.optional),
    field("gui", block([item(primitive("scalar"), occurs.any)]), occurs.one),
    field(
      "types",
      block([item(literal("country"), occurs.optional), item(literal("planet"), occurs.optional)]),
      occurs.optional,
    ),
    field("text", block([item(primitive("localisation"), occurs.any)]), occurs.optional),
    field("texture", block([item(primitive("scalar"), occurs.any)]), occurs.optional),
    field("fire_only_once", primitive("boolean"), occurs.optional),
    field("gui", block([item(primitive("scalar"), occurs.any)]), occurs.one),
    field(
      "types",
      block([item(literal("country"), occurs.optional), item(literal("planet"), occurs.optional)]),
      occurs.optional,
    ),
    field("text", block([item(primitive("localisation"), occurs.any)]), occurs.optional),
    field("texture", block([item(primitive("scalar"), occurs.any)]), occurs.optional),
  ],
});
