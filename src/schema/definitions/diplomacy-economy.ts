// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  anyKey,
  block,
  defineType,
  field,
  fileDefinitions,
  occurs,
  replaceScope,
  ruleSetEntries,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const diplomacyEconomy: DefinitionType = defineType({
  id: DefinitionTypeId.DiplomacyEconomy,
  source: fileDefinitions("common/diplomacy_economy"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field(
      "once",
      block([
        field(
          anyKey(),
          block([
            field("trigger", block([triggerEntries()]), occurs.one, {
              scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
            }),
            field(
              "resources",
              block([field("category", typeRef("economic_category"), occurs.one), ruleSetEntries("economic_template")]),
              occurs.one,
            ),
          ]),
          occurs.any,
        ),
      ]),
      occurs.one,
    ),
    field(
      "per_relation",
      block([
        field(
          anyKey(),
          block([
            field("trigger", block([triggerEntries()]), occurs.one, {
              scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
            }),
            field(
              "resources",
              block([field("category", typeRef("economic_category"), occurs.one), ruleSetEntries("economic_template")]),
              occurs.one,
            ),
          ]),
          occurs.any,
        ),
      ]),
      occurs.one,
    ),
    field(
      "once",
      block([
        field(
          anyKey(),
          block([
            field("trigger", block([triggerEntries()]), occurs.one, {
              scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
            }),
            field(
              "resources",
              block([field("category", typeRef("economic_category"), occurs.one), ruleSetEntries("economic_template")]),
              occurs.one,
            ),
          ]),
          occurs.any,
        ),
      ]),
      occurs.one,
    ),
    field(
      "per_relation",
      block([
        field(
          anyKey(),
          block([
            field("trigger", block([triggerEntries()]), occurs.one, {
              scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
            }),
            field(
              "resources",
              block([field("category", typeRef("economic_category"), occurs.one), ruleSetEntries("economic_template")]),
              occurs.one,
            ),
          ]),
          occurs.any,
        ),
      ]),
      occurs.one,
    ),
  ],
});
