// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  definitionLocalisation,
  effectEntries,
  field,
  keyedBlocks,
  modifierEntries,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  ruleSetEntries,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const starbaseModule: DefinitionType = defineType({
  id: DefinitionTypeId.StarbaseModule,
  source: keyedBlocks("common/starbase_modules"),
  variants: [],
  localisation: [
    definitionLocalisation("Name", "sm_$", true),
    definitionLocalisation("Description", "sm_$_desc", true),
  ],
  modifiers: [],
  entries: [
    field("section", typeRef("section_template"), occurs.optional),
    field("icon", typeRef("sprite"), occurs.optional),
    field("construction_days", primitive("integer"), occurs.optional),
    field("starbase_type", typeRef("starbase_type"), occurs.optional),
    field("starbase_type", typeRef("megastructure"), occurs.optional),
    field("initial", primitive("boolean"), occurs.optional),
    field("ai_build_at_chokepoint", primitive("boolean"), occurs.optional),
    field("ai_build_outside_chokepoint", primitive("boolean"), occurs.optional),
    field("show_in_tech", typeRef("technology"), occurs.any),
    field("component_set", typeRef("component_set"), occurs.optional),
    field(
      "resources",
      block([field("category", typeRef("economic_category"), occurs.one), ruleSetEntries("economic_template")]),
      occurs.one,
    ),
    field("potential", block([triggerEntries()]), occurs.optional),
    field("possible", block([triggerEntries()]), occurs.optional, {
      documentation: "entries in possible are displayed in game. Use custom_tooltip to replace localisations.",
    }),
    field("equipped_component", typeRef("component_template"), occurs.optional),
    field("custom_tooltip", primitive("localisation"), occurs.any),
    field(
      "station_modifier",
      block([
        field("custom_tooltip", primitive("localisation"), occurs.optional),
        field("show_only_custom_tooltip", primitive("boolean"), occurs.optional),
        modifierEntries(),
      ]),
      occurs.optional,
      {
        documentation: "ship modifiers, apply on the starbase itself",
        scope: replaceScope({ root: ScopeId.Starbase }),
      },
    ),
    field("defense_platform_modifier", block([modifierEntries()]), occurs.optional, {
      documentation: "ship modifiers, apply to defensive platforms",
      scope: replaceScope({ root: ScopeId.Ship }),
    }),
    field("ship_modifier", block([modifierEntries()]), occurs.optional, {
      documentation: "ship modifiers, apply in orbit of the starbase",
      scope: replaceScope({ root: ScopeId.Ship }),
    }),
    field("orbit_modifier", block([modifierEntries()]), occurs.optional, {
      documentation: "ship modifiers, apply in orbit of the starbase",
      scope: replaceScope({ root: ScopeId.Ship }),
    }),
    field(
      "system_modifier",
      block([
        modifierEntries(),
        field("custom_tooltip", primitive("localisation"), occurs.optional),
        field("show_only_custom_tooltip", primitive("boolean"), occurs.optional),
      ]),
      occurs.optional,
      { documentation: "planet modifiers, apply on system planets.", scope: replaceScope({ root: ScopeId.Planet }) },
    ),
    field("country_modifier", block([modifierEntries()]), occurs.optional, {
      documentation: "country modifiers",
      scope: replaceScope({ root: ScopeId.Country }),
    }),
    field(
      "planet_modifier",
      block([
        modifierEntries(),
        field("custom_tooltip", primitive("localisation"), occurs.optional),
        field("show_only_custom_tooltip", primitive("boolean"), occurs.optional),
      ]),
      occurs.optional,
      { documentation: "planet modifiers (orbital ring)", scope: replaceScope({ root: ScopeId.Planet }) },
    ),
    ruleSetEntries("triggered_planet_modifier_starbase"),
    ruleSetEntries("triggered_system_modifier_starbase"),
    ruleSetEntries("triggered_country_modifier_starbase"),
    ruleSetEntries("triggered_station_modifier_starbase"),
    field("ai_weight", block([modifierRuleEntries()]), occurs.optional),
    field("replaceable", block([triggerEntries()]), occurs.optional),
    field("on_finished", block([effectEntries()]), occurs.optional),
    field("on_destroyed", block([effectEntries()]), occurs.optional),
    field("abort_construction_trigger", block([triggerEntries()]), occurs.optional),
  ],
});
