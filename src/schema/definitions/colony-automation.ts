// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  anyKey,
  block,
  defineType,
  enumRef,
  field,
  item,
  keyedBlocks,
  occurs,
  primitive,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const colonyAutomation: DefinitionType = defineType({
  id: DefinitionTypeId.ColonyAutomation,
  source: keyedBlocks("common/colony_automation", false),
  entryScope: ScopeId.Planet,
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("category", primitive("scalar"), occurs.one),
    field("available", block([triggerEntries()]), occurs.one),
    field("prio_districts", block([item(typeRef("district"), occurs.any)]), occurs.optional),
    field(
      "prio_zones",
      block([item(typeRef("zones"), occurs.any), item(enumRef(EnumId.ZoneType), occurs.any)]),
      occurs.optional,
    ),
    field(
      "buildings",
      block([
        field(
          anyKey(),
          block([
            field("building", typeRef("building"), occurs.one),
            field("available", block([triggerEntries()]), occurs.optional),
            field("planet_limit", primitive("integer"), occurs.optional),
          ]),
          occurs.oneOrMore,
        ),
      ]),
      occurs.one,
    ),
    field("category", primitive("scalar"), occurs.one),
    field("available", block([triggerEntries()]), occurs.one),
    field("prio_districts", block([item(typeRef("district"), occurs.any)]), occurs.optional),
    field(
      "prio_zones",
      block([item(typeRef("zones"), occurs.any), item(enumRef(EnumId.ZoneType), occurs.any)]),
      occurs.optional,
    ),
    field(
      "buildings",
      block([
        field(
          anyKey(),
          block([
            field("building", typeRef("building"), occurs.one),
            field("available", block([triggerEntries()]), occurs.optional),
            field("planet_limit", primitive("integer"), occurs.optional),
          ]),
          occurs.oneOrMore,
        ),
      ]),
      occurs.one,
    ),
  ],
});
