// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  between,
  block,
  defineType,
  definitionLocalisation,
  effectEntries,
  enumRef,
  field,
  item,
  keyedBlocks,
  literal,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const diplomaticAction: DefinitionType = defineType({
  id: DefinitionTypeId.DiplomaticAction,
  source: keyedBlocks("common/diplomatic_actions"),
  variants: [],
  localisation: [
    definitionLocalisation("Name", "_TITLE", false),
    definitionLocalisation("Description", "_DESC", false),
    definitionLocalisation("Long desc", "_LONG_DESC", false),
    definitionLocalisation("Type", "_TYPE", false),
    definitionLocalisation("Type desc", "_TYPE_DESC", false),
  ],
  modifiers: [],
  entries: [
    field("icon", typeRef("sprite"), occurs.optional),
    field("action_type", enumRef(EnumId.ActionTypes), occurs.optional),
    field("require_envoy", literal(true), occurs.optional),
    field("envoy_assignment", primitive("localisation"), occurs.optional),
    field("is_listed", primitive("boolean"), occurs.optional, {
      documentation: "decides if this diplomactic action type is listed in diplomatic view.",
    }),
    field("should_remove_response_message_when_not_possible", primitive("boolean"), occurs.optional),
    field("prerequisites", block([item(typeRef("technology"), between(0, 100))]), occurs.optional, {
      documentation: "the technologies required to enable the player to send this diplomatic action.",
    }),
    field("requires_actor_peace", primitive("boolean"), occurs.optional, {
      documentation: "will require the actor to be at peace for this action to pass the possible check.",
    }),
    field("show_to_alliance_members", primitive("boolean"), occurs.optional),
    field("requires_alliance_vote", primitive("boolean"), occurs.optional, {
      documentation:
        "will require the alliance of the actor to vote for this action before passing it on to the recipient.",
    }),
    field("requires_unanimous_vote", primitive("boolean"), occurs.optional),
    field("requires_actor_independence", primitive("boolean"), occurs.optional, {
      documentation: "true by default, will require the actor to be independent.",
    }),
    field("requires_recipient_independence", primitive("boolean"), occurs.optional, {
      documentation: "true by default, will require the recipient to be independent.",
    }),
    field("requires_actor_federation_leader", primitive("boolean"), occurs.optional, {
      documentation: "false by default, if the actor is in a federation, he has to be the leader of it.",
    }),
    field("requires_recipient_federation_leader", primitive("boolean"), occurs.optional, {
      documentation: "true by default, if the recipient is in a federation, he has to be the leader of it.",
    }),
    field("requires_recipient_peace", primitive("boolean"), occurs.optional),
    field("requires_recipient_alliance_vote", primitive("boolean"), occurs.optional),
    field("show_decline_to_alliance_members", primitive("boolean"), occurs.optional, {
      documentation: "false by default, will show your alliance members when you decline a proposal of this type.",
    }),
    field("should_show_accept_message", primitive("boolean"), occurs.optional, {
      documentation: "decides whether actor gets a response on acceptance (otherwise just on decline)",
    }),
    field("should_show_auto_accept_message_recipient", primitive("boolean"), occurs.optional, {
      documentation: "decides if the recipient gets a message showing the diplomatic action.",
    }),
    field("should_open_auto_accept_message_recipient", primitive("boolean"), occurs.optional, {
      documentation: "decides if the recipient automatically opens this message.",
    }),
    field("should_notify_auto_recipient_on_vote_fail", primitive("boolean"), occurs.optional),
    field("should_show_auto_accept_message_actor", primitive("boolean"), occurs.optional, {
      documentation: "decides if the actor gets a reply showing the diplomatic action.",
    }),
    field("should_notify_all_communications", primitive("boolean"), occurs.optional, {
      documentation:
        "decides if a message notification is sent to all countries who have communications with actor or recipient",
    }),
    field("diplo_view_acceptance_icon", primitive("boolean"), occurs.optional),
    field("auto_accepted", primitive("boolean"), occurs.optional),
    field("AI_acceptance_base_value", primitive("integer"), occurs.optional),
    field("sound", typeRef("sound_effect"), occurs.optional),
    field("potential", block([triggerEntries()]), occurs.optional, {
      documentation: "root is actor, from is recipient, decides if the action is shown",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("possible", block([triggerEntries()]), occurs.optional, {
      documentation: "root is actor, from is recipient, decides if the action is still possible",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("proposable", block([triggerEntries()]), occurs.optional, {
      documentation: "root is actor, from is recipient, decides if the action is proposable",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_accept", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_decline", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("ai_acceptance", block([modifierRuleEntries()]), occurs.optional, {
      documentation:
        "weight field where you can define AI reasons to accept or reject the proposal. root is recipient, from is actor. Does not overwrite hardcoded reasons, and only using add and subtract is recommended, since multiplication won't affect reasons outside the ai_acceptance field",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("should_ai_propose", block([modifierRuleEntries()]), occurs.optional, {
      documentation:
        "weight field where you can define AI reasons to propose the proposal. root is actor, from is recipient.",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
  ],
});
