// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  between,
  block,
  defineType,
  definitionLocalisation,
  enumRef,
  field,
  item,
  keyedBlocks,
  literal,
  modifierEntries,
  modifierRuleEntries,
  noScope,
  occurs,
  primitive,
  replaceScope,
  ruleSetEntries,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const authority: DefinitionType = defineType({
  id: DefinitionTypeId.Authority,
  source: keyedBlocks("common/governments/authorities"),
  variants: [],
  localisation: [definitionLocalisation("Name", "", true), definitionLocalisation("Description", "_desc", true)],
  modifiers: [],
  entries: [
    field("machine_empire", literal(true), occurs.optional),
    field("icon", primitive("file"), occurs.optional),
    field(
      "potential",
      block([
        field("text", primitive("localisation"), occurs.optional),
        field("always", primitive("boolean"), occurs.optional),
        ruleSetEntries("government_trigger"),
      ]),
      occurs.optional,
    ),
    field(
      "possible",
      block([
        field("text", primitive("localisation"), occurs.optional),
        field("always", primitive("boolean"), occurs.optional),
        ruleSetEntries("government_trigger"),
      ]),
      occurs.optional,
    ),
    field("random_weight", block([modifierRuleEntries()]), occurs.any, { scope: replaceScope({ root: noScope() }) }),
    field(
      "advanced_authority_swap",
      block([
        field("name", primitive("localisation"), occurs.one),
        field("description", primitive("localisation"), occurs.one),
        field("inherit_icon", primitive("boolean"), occurs.one),
        field("inherit_effects", primitive("boolean"), occurs.one),
        field("has_heir", primitive("boolean"), occurs.optional),
        field("ruler_council_position", typeRef("councilor"), occurs.optional),
        field("trigger", block([triggerEntries()]), occurs.one),
        field(
          "modifier",
          block([
            field("custom_tooltip", primitive("localisation"), occurs.optional),
            field("show_only_custom_tooltip", primitive("boolean"), occurs.optional),
            modifierEntries(),
          ]),
          occurs.one,
        ),
        field("tags", block([item(primitive("localisation"), occurs.oneOrMore)]), occurs.optional),
      ]),
      occurs.any,
    ),
    field("traits", block([field("trait", typeRef("trait"), occurs.any)]), occurs.optional),
    field(
      "country_modifier",
      block([
        modifierEntries(),
        field("custom_tooltip", primitive("localisation"), occurs.optional),
        field("show_only_custom_tooltip", literal(false), occurs.optional),
      ]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field("playable", block([triggerEntries()]), occurs.optional),
    field("has_heir", primitive("boolean"), occurs.optional),
    field("election_type", enumRef(EnumId.ElectionTypes), occurs.optional),
    field("election_term_years", primitive("integer"), occurs.optional, {
      documentation: "default = 0, hence required if defined to oligarchic or democratic",
    }),
    field("election_term_variance", primitive("integer"), occurs.optional, { documentation: "acts like random = int" }),
    field("re_election_allowed", primitive("boolean"), occurs.optional),
    field("can_have_emergency_elections", primitive("boolean"), occurs.optional),
    field("max_election_candidates", primitive("integer"), occurs.optional, {
      documentation: "default = -1, which equals limitless",
    }),
    field("can_reform", primitive("boolean"), occurs.optional),
    field("has_agendas", primitive("boolean"), occurs.optional, {
      documentation:
        "is tied to the agenda files if = yes (note: this property has not been removed from existing authorities, although agendas have been removed as objects)",
    }),
    field("ruler_council_position", typeRef("councilor"), occurs.one),
    field("has_factions", primitive("boolean"), occurs.optional),
    field("valid_for_released_vassal", primitive("boolean"), occurs.optional),
    field("localization_postfix", enumRef(EnumId.AuthorityPostfixes), occurs.optional),
    field("localization_postfix", primitive("scalar"), occurs.optional),
    field("trait_localization_postfix", primitive("scalar"), occurs.optional),
    field("uses_mandates", primitive("boolean"), occurs.optional),
    field("color", block([item(primitive("integer"), between(4, 4))]), occurs.optional),
    field("tags", block([item(primitive("localisation"), occurs.oneOrMore)]), occurs.optional),
    field("machine_empire", literal(true), occurs.optional),
    field("icon", primitive("file"), occurs.optional),
    field(
      "potential",
      block([
        field("text", primitive("localisation"), occurs.optional),
        field("always", primitive("boolean"), occurs.optional),
        ruleSetEntries("government_trigger"),
      ]),
      occurs.optional,
    ),
    field(
      "possible",
      block([
        field("text", primitive("localisation"), occurs.optional),
        field("always", primitive("boolean"), occurs.optional),
        ruleSetEntries("government_trigger"),
      ]),
      occurs.optional,
    ),
    field("random_weight", block([modifierRuleEntries()]), occurs.any, { scope: replaceScope({ root: noScope() }) }),
    field(
      "advanced_authority_swap",
      block([
        field("name", primitive("localisation"), occurs.one),
        field("description", primitive("localisation"), occurs.one),
        field("inherit_icon", primitive("boolean"), occurs.one),
        field("inherit_effects", primitive("boolean"), occurs.one),
        field("has_heir", primitive("boolean"), occurs.optional),
        field("ruler_council_position", typeRef("councilor"), occurs.optional),
        field("trigger", block([triggerEntries()]), occurs.one),
        field(
          "modifier",
          block([
            field("custom_tooltip", primitive("localisation"), occurs.optional),
            field("show_only_custom_tooltip", primitive("boolean"), occurs.optional),
            modifierEntries(),
          ]),
          occurs.one,
        ),
        field("tags", block([item(primitive("localisation"), occurs.oneOrMore)]), occurs.optional),
      ]),
      occurs.any,
    ),
    field("traits", block([field("trait", typeRef("trait"), occurs.any)]), occurs.optional),
    field(
      "country_modifier",
      block([
        modifierEntries(),
        field("custom_tooltip", primitive("localisation"), occurs.optional),
        field("show_only_custom_tooltip", literal(false), occurs.optional),
      ]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field("playable", block([triggerEntries()]), occurs.optional),
    field("has_heir", primitive("boolean"), occurs.optional),
    field("election_type", enumRef(EnumId.ElectionTypes), occurs.optional),
    field("election_term_years", primitive("integer"), occurs.optional, {
      documentation: "default = 0, hence required if defined to oligarchic or democratic",
    }),
    field("election_term_variance", primitive("integer"), occurs.optional, { documentation: "acts like random = int" }),
    field("re_election_allowed", primitive("boolean"), occurs.optional),
    field("can_have_emergency_elections", primitive("boolean"), occurs.optional),
    field("max_election_candidates", primitive("integer"), occurs.optional, {
      documentation: "default = -1, which equals limitless",
    }),
    field("can_reform", primitive("boolean"), occurs.optional),
    field("has_agendas", primitive("boolean"), occurs.optional, {
      documentation:
        "is tied to the agenda files if = yes (note: this property has not been removed from existing authorities, although agendas have been removed as objects)",
    }),
    field("ruler_council_position", typeRef("councilor"), occurs.one),
    field("has_factions", primitive("boolean"), occurs.optional),
    field("valid_for_released_vassal", primitive("boolean"), occurs.optional),
    field("localization_postfix", enumRef(EnumId.AuthorityPostfixes), occurs.optional),
    field("localization_postfix", primitive("scalar"), occurs.optional),
    field("trait_localization_postfix", primitive("scalar"), occurs.optional),
    field("uses_mandates", primitive("boolean"), occurs.optional),
    field("color", block([item(primitive("integer"), between(4, 4))]), occurs.optional),
    field("tags", block([item(primitive("localisation"), occurs.oneOrMore)]), occurs.optional),
  ],
});
