// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId } from "../catalog.js";

import { block, defineType, enumKey, enumRef, field, keyedBlocks, literal, occurs, primitive } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const componentSlotTemplate: DefinitionType = defineType({
  id: DefinitionTypeId.ComponentSlotTemplate,
  source: keyedBlocks("common/component_slot_templates"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("size", enumRef(EnumId.WeaponSlotSize), occurs.one),
    field("component", literal("weapon"), occurs.one),
    field("component", literal("strike_craft"), occurs.one),
    field("component", literal("any"), occurs.one),
    field("is_fixed", literal(true), occurs.optional),
    field(
      "entities",
      block([field(enumKey(EnumId.WeaponType), primitive("scalar"), occurs.oneOrMore)]),
      occurs.optional,
    ),
    field("size", enumRef(EnumId.WeaponSlotSize), occurs.one),
    field("component", literal("weapon"), occurs.one),
    field("component", literal("strike_craft"), occurs.one),
    field("component", literal("any"), occurs.one),
    field("is_fixed", literal(true), occurs.optional),
    field(
      "entities",
      block([field(enumKey(EnumId.WeaponType), primitive("scalar"), occurs.oneOrMore)]),
      occurs.optional,
    ),
  ],
});
