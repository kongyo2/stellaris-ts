// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  enumKey,
  field,
  keyedBlocks,
  occurs,
  primitive,
  scriptValue,
  triggerEntries,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const patronsDeeds: DefinitionType = defineType({
  id: DefinitionTypeId.PatronsDeeds,
  source: keyedBlocks("common/patrons/deeds", false),
  entryScope: ScopeId.Country,
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("attunement", scriptValue("number"), occurs.optional),
    field("has_desc", primitive("boolean"), occurs.optional),
    field(
      "counter",
      block([
        field(enumKey(EnumId.PatronsDeedsPo), block([field("max", primitive("integer"), occurs.one)]), occurs.one),
      ]),
      occurs.optional,
    ),
    field("potential", block([triggerEntries()]), occurs.optional),
  ],
});
