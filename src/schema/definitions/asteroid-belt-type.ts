// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { defineType, field, keyedBlocks, occurs, primitive } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const asteroidBeltType: DefinitionType = defineType({
  id: DefinitionTypeId.AsteroidBeltType,
  source: keyedBlocks("common/asteroid_belts"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("mesh", primitive("scalar"), occurs.oneOrMore),
    field("shader", primitive("scalar"), occurs.optional),
    field("width", primitive("number"), occurs.optional),
    field("density", primitive("number"), occurs.optional),
  ],
});
