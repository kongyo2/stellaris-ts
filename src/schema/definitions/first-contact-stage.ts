// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  effectEntries,
  field,
  keyedBlocks,
  modifierRuleEntries,
  occurs,
  primitive,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const firstContactStage: DefinitionType = defineType({
  id: DefinitionTypeId.FirstContactStage,
  source: keyedBlocks("common/first_contact"),
  entryScope: ScopeId.FirstContact,
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("picture", typeRef("sprite"), occurs.optional),
    field("icon", typeRef("sprite"), occurs.optional),
    field("difficulty", primitive("integer", { min: null, max: null }), occurs.one),
    field(
      "difficulty",
      block([
        field("min", primitive("integer", { min: null, max: null }), occurs.one),
        field("max", primitive("integer", { min: null, max: null }), occurs.one),
      ]),
      occurs.one,
    ),
    field(
      "stage_event",
      block([
        field("icon", typeRef("sprite"), occurs.optional),
        field("summary", primitive("localisation"), occurs.optional),
        field("event_weight", block([modifierRuleEntries()]), occurs.one),
        field("event", typeRef("event", "first_contact"), occurs.one),
      ]),
      occurs.oneOrMore,
    ),
    field("on_roll_failed", block([effectEntries()]), occurs.one),
  ],
});
