// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  effectEntries,
  field,
  keyedBlocks,
  literal,
  modifierRuleEntries,
  occurs,
  primitive,
  ruleSetEntries,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const astralAction: DefinitionType = defineType({
  id: DefinitionTypeId.AstralAction,
  source: keyedBlocks("common/astral_actions"),
  entryScope: ScopeId.Country,
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("sound", typeRef("sound_effect"), occurs.optional),
    field("unlock_threshold", primitive("integer"), occurs.optional),
    field("usages", primitive("integer"), occurs.optional),
    field("custom_usage_tooltip", primitive("localisation"), occurs.optional),
    field("upgrade", typeRef("astral_action"), occurs.optional),
    field("activation_cost", block([ruleSetEntries("economic_template")]), occurs.optional),
    field("cooldown", primitive("integer"), occurs.optional),
    field("uses_custom_cooldown", literal(true), occurs.optional),
    field("picture", typeRef("sprite"), occurs.one),
    field("potential", block([triggerEntries()]), occurs.optional),
    field("allow", block([triggerEntries()]), occurs.optional),
    field("allow_unlock", block([triggerEntries()]), occurs.optional),
    field("is_exhausted", block([triggerEntries()]), occurs.optional),
    field("effect", block([effectEntries()]), occurs.optional),
    field("ai_weight", block([modifierRuleEntries()]), occurs.optional),
    field("sound", typeRef("sound_effect"), occurs.optional),
    field("unlock_threshold", primitive("integer"), occurs.optional),
    field("usages", primitive("integer"), occurs.optional),
    field("custom_usage_tooltip", primitive("localisation"), occurs.optional),
    field("upgrade", typeRef("astral_action"), occurs.optional),
    field("activation_cost", block([ruleSetEntries("economic_template")]), occurs.optional),
    field("cooldown", primitive("integer"), occurs.optional),
    field("uses_custom_cooldown", literal(true), occurs.optional),
    field("picture", typeRef("sprite"), occurs.one),
    field("potential", block([triggerEntries()]), occurs.optional),
    field("allow", block([triggerEntries()]), occurs.optional),
    field("allow_unlock", block([triggerEntries()]), occurs.optional),
    field("is_exhausted", block([triggerEntries()]), occurs.optional),
    field("effect", block([effectEntries()]), occurs.optional),
    field("ai_weight", block([modifierRuleEntries()]), occurs.optional),
  ],
});
