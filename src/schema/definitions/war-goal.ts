// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  definitionLocalisation,
  effectEntries,
  enumRef,
  field,
  keyedBlocks,
  literal,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const warGoal: DefinitionType = defineType({
  id: DefinitionTypeId.WarGoal,
  source: keyedBlocks("common/war_goals"),
  variants: [],
  localisation: [
    definitionLocalisation("Name", "war_goal_$", true),
    definitionLocalisation("Desc", "war_goal_$_desc", true),
  ],
  modifiers: [],
  entries: [
    field("casus_belli", typeRef("casus_belli"), occurs.one),
    field("cede_claims", enumRef(EnumId.CedeClaims), occurs.optional, {
      documentation: "Cede claims on peace? Yes by default (I think - Caligula)",
    }),
    field("defender_default", primitive("boolean"), occurs.optional, {
      documentation:
        "Defenders get this war goal if no war goal is picked in time. If multiple war goals are set to be defender default, the first one scripted in the file is chosen. No by default.",
    }),
    field("proxy_wars_only", primitive("boolean"), occurs.optional),
    field("threat_multiplier", primitive("number"), occurs.optional, {
      documentation: "Affects threat generated by conquering systems & planets. 1.0 by default.",
    }),
    field("show_claims_in_description", primitive("boolean"), occurs.optional, {
      documentation: "This decides whether claims should be listed in the description. No by default.",
    }),
    field("total_war", primitive("boolean"), occurs.optional),
    field("hide_if_no_cb", primitive("boolean"), occurs.optional, {
      documentation: "Hide this wargoal if country doesn't have the requisite CB. No by default.",
    }),
    field("secret_fealties_join_attacker", primitive("boolean"), occurs.optional, {
      documentation: "Will countries that have sworn secret fealty to the attacker join the war? No by default.",
    }),
    field("surrender_acceptance", primitive("integer"), occurs.optional, {
      documentation:
        "This affects how likely the AI is to surrender to this wargoal, generally less willing on more severe wargoals (negative numbers = less willing). 0 by default.",
    }),
    field("war_exhaustion", primitive("number"), occurs.optional, {
      documentation:
        "This affects how quickly war exhaustion builds up for this side in the war (2.0 = twice as fast). 1.0 by default.",
    }),
    field("set_defender_wargoal", typeRef("war_goal"), occurs.optional, {
      documentation:
        "If used as attacker wargoal and set, defender will have their wargoal predetermined by this. If not set, defender gets to pick their own wargoal.",
    }),
    field("release_occupied_systems_on_status_quo", primitive("boolean"), occurs.optional),
    field("show_agreement_terms", primitive("boolean"), occurs.optional, {
      documentation:
        "If set to 'yes', then the default agreement terms will be shown in a tooltip when hovering over the war goal description",
    }),
    field("potential", block([triggerEntries()]), occurs.optional, {
      documentation: "FROM = The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("possible", block([triggerEntries()]), occurs.optional, {
      documentation: "FROM = The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_status_quo", block([effectEntries()]), occurs.optional, {
      documentation: "Run when war ends in status quo. The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_wargoal_set", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_accept", block([effectEntries()]), occurs.optional, {
      documentation:
        "Run when target surrenders after claims are ceded. The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("ai_weight", block([modifierRuleEntries()]), occurs.optional, {
      documentation: "FROM = The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("galactic_empire_joins_defender", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field(
      "forbidden_peace_offers",
      block([field("demand_surrender", literal(""), occurs.one), field("surrender", literal(""), occurs.one)]),
      occurs.optional,
    ),
    field("forbidden_peace_offers", block([field("demand_surrender", literal(""), occurs.one)]), occurs.optional),
    field("forbidden_peace_offers", block([field("surrender", literal(""), occurs.one)]), occurs.optional),
    field(
      "forbidden_peace_offers",
      block([
        field("demand_surrender", literal(""), occurs.one),
        field("status_quo", literal(""), occurs.one),
        field("surrender", literal(""), occurs.one),
      ]),
      occurs.optional,
    ),
    field("forbidden_peace_offers", block([field("status_quo", literal(""), occurs.one)]), occurs.optional),
    field("destroy_starbases", literal(true), occurs.optional, {
      documentation: "Starbases are destroyed instead of ownership being transferred",
    }),
    field("should_ignore_federations", primitive("boolean"), occurs.optional),
    field("casus_belli", typeRef("casus_belli"), occurs.one),
    field("cede_claims", enumRef(EnumId.CedeClaims), occurs.optional, {
      documentation: "Cede claims on peace? Yes by default (I think - Caligula)",
    }),
    field("defender_default", primitive("boolean"), occurs.optional, {
      documentation:
        "Defenders get this war goal if no war goal is picked in time. If multiple war goals are set to be defender default, the first one scripted in the file is chosen. No by default.",
    }),
    field("proxy_wars_only", primitive("boolean"), occurs.optional),
    field("threat_multiplier", primitive("number"), occurs.optional, {
      documentation: "Affects threat generated by conquering systems & planets. 1.0 by default.",
    }),
    field("show_claims_in_description", primitive("boolean"), occurs.optional, {
      documentation: "This decides whether claims should be listed in the description. No by default.",
    }),
    field("total_war", primitive("boolean"), occurs.optional),
    field("hide_if_no_cb", primitive("boolean"), occurs.optional, {
      documentation: "Hide this wargoal if country doesn't have the requisite CB. No by default.",
    }),
    field("secret_fealties_join_attacker", primitive("boolean"), occurs.optional, {
      documentation: "Will countries that have sworn secret fealty to the attacker join the war? No by default.",
    }),
    field("surrender_acceptance", primitive("integer"), occurs.optional, {
      documentation:
        "This affects how likely the AI is to surrender to this wargoal, generally less willing on more severe wargoals (negative numbers = less willing). 0 by default.",
    }),
    field("war_exhaustion", primitive("number"), occurs.optional, {
      documentation:
        "This affects how quickly war exhaustion builds up for this side in the war (2.0 = twice as fast). 1.0 by default.",
    }),
    field("set_defender_wargoal", typeRef("war_goal"), occurs.optional, {
      documentation:
        "If used as attacker wargoal and set, defender will have their wargoal predetermined by this. If not set, defender gets to pick their own wargoal.",
    }),
    field("release_occupied_systems_on_status_quo", primitive("boolean"), occurs.optional),
    field("show_agreement_terms", primitive("boolean"), occurs.optional, {
      documentation:
        "If set to 'yes', then the default agreement terms will be shown in a tooltip when hovering over the war goal description",
    }),
    field("potential", block([triggerEntries()]), occurs.optional, {
      documentation: "FROM = The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("possible", block([triggerEntries()]), occurs.optional, {
      documentation: "FROM = The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_status_quo", block([effectEntries()]), occurs.optional, {
      documentation: "Run when war ends in status quo. The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_wargoal_set", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("on_accept", block([effectEntries()]), occurs.optional, {
      documentation:
        "Run when target surrenders after claims are ceded. The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("ai_weight", block([modifierRuleEntries()]), occurs.optional, {
      documentation: "FROM = The targeted country, THIS = The acting country",
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field("galactic_empire_joins_defender", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country, from: ScopeId.Country }),
    }),
    field(
      "forbidden_peace_offers",
      block([field("demand_surrender", literal(""), occurs.one), field("surrender", literal(""), occurs.one)]),
      occurs.optional,
    ),
    field("forbidden_peace_offers", block([field("demand_surrender", literal(""), occurs.one)]), occurs.optional),
    field("forbidden_peace_offers", block([field("surrender", literal(""), occurs.one)]), occurs.optional),
    field(
      "forbidden_peace_offers",
      block([
        field("demand_surrender", literal(""), occurs.one),
        field("status_quo", literal(""), occurs.one),
        field("surrender", literal(""), occurs.one),
      ]),
      occurs.optional,
    ),
    field("forbidden_peace_offers", block([field("status_quo", literal(""), occurs.one)]), occurs.optional),
    field("destroy_starbases", literal(true), occurs.optional, {
      documentation: "Starbases are destroyed instead of ownership being transferred",
    }),
    field("should_ignore_federations", primitive("boolean"), occurs.optional),
  ],
});
