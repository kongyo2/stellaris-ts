// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  between,
  block,
  defineType,
  definitionLocalisation,
  enumRef,
  field,
  fieldPresent,
  item,
  literal,
  modifierEntries,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  ruleSetEntries,
  taggedBlocks,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const sectionTemplate: DefinitionType = defineType({
  id: DefinitionTypeId.SectionTemplate,
  source: taggedBlocks("common/section_templates", "key", []),
  variants: [{ id: "needs_locs", when: fieldPresent("ship_size") }],
  localisation: [definitionLocalisation("Name", "", true, "needs_locs")],
  modifiers: [],
  entries: [
    field("key", primitive("scalar"), occurs.one),
    field("ship_size", typeRef("ship_size"), occurs.any),
    field("fits_on_slot", enumRef(EnumId.SectionSlots), occurs.any),
    field("entity", primitive("scalar"), occurs.one),
    field("icon", primitive("scalar"), occurs.optional, {
      documentation:
        "As far as I can tell, this is unused (the Vanilla ones refer to non-existent sprites and I couldn't find an ingame effect) -Caligula",
    }),
    field("icon_frame", primitive("integer"), occurs.optional),
    field("compatible_with_all_ship_sizes", primitive("boolean"), occurs.optional),
    field(
      "ai_tags",
      block([item(enumRef(EnumId.ComponentTags), occurs.any), item(literal("citadel"), occurs.optional)]),
      occurs.optional,
    ),
    field("should_draw_components", primitive("boolean"), occurs.optional),
    field(
      "resources",
      block([field("category", typeRef("economic_category"), occurs.one), ruleSetEntries("economic_template")]),
      occurs.optional,
    ),
    field(
      "prerequisites",
      block([
        item(typeRef("technology"), occurs.any),
        field("OR", block([item(typeRef("technology"), between(2, null))]), occurs.any),
      ]),
      occurs.optional,
    ),
    field(
      "component_slot",
      block([
        field("name", primitive("scalar"), occurs.one),
        field("template", typeRef("component_slot_template"), occurs.one),
        field("locatorname", enumRef(EnumId.Locators), occurs.optional),
        field("locatorname", primitive("scalar"), occurs.optional),
        field("is_side_slot", primitive("boolean"), occurs.optional),
        field("rotation", primitive("integer"), occurs.optional),
        field(
          "position",
          block([field("x", primitive("number"), occurs.one), field("y", primitive("number"), occurs.one)]),
          occurs.optional,
        ),
        field("section_instance", primitive("integer"), occurs.optional),
      ]),
      occurs.any,
    ),
    field("small_utility_slots", primitive("integer"), occurs.optional),
    field("medium_utility_slots", primitive("integer"), occurs.optional),
    field("large_utility_slots", primitive("integer"), occurs.optional),
    field("aux_utility_slots", primitive("integer"), occurs.optional),
    field("ai_weight", block([modifierRuleEntries()]), occurs.optional),
    field("modifier", block([modifierEntries()]), occurs.optional, { scope: replaceScope({ root: ScopeId.Ship }) }),
    field("ship_modifier", block([modifierEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Ship }),
    }),
  ],
});
