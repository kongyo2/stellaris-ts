// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  effectEntries,
  enumKey,
  field,
  keyedBlocks,
  occurs,
  primitive,
  scriptValue,
  triggerEntries,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const patronsCallings: DefinitionType = defineType({
  id: DefinitionTypeId.PatronsCallings,
  source: keyedBlocks("common/patrons/callings", false),
  entryScope: ScopeId.Country,
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("potential", block([triggerEntries()]), occurs.optional),
    field("first_tier", primitive("integer"), occurs.optional),
    field(
      "tier",
      block([
        field("attunement", scriptValue("number"), occurs.one),
        field("trigger", block([triggerEntries()]), occurs.optional),
        field(
          "counter",
          block([
            field(
              enumKey(EnumId.PatronsCallingsPo),
              block([field("max", primitive("integer"), occurs.one)]),
              occurs.one,
            ),
          ]),
          occurs.optional,
        ),
      ]),
      occurs.any,
    ),
    field("on_start", block([effectEntries()]), occurs.optional),
    field("on_dismissed", block([effectEntries()]), occurs.optional),
  ],
});
