// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId } from "../catalog.js";

import { defineType, field, occurs, primitive, taggedBlocks } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const modelAnimation: DefinitionType = defineType({
  id: DefinitionTypeId.ModelAnimation,
  source: taggedBlocks("gfx/models", "name", [], true, { rootKeyFilter: { mode: "include", values: ["animation"] } }),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [field("name", primitive("scalar"), occurs.one), field("file", primitive("scalar"), occurs.one)],
});
