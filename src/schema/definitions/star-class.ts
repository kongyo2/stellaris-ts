// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  definitionLocalisation,
  field,
  keyedBlocks,
  modifierEntries,
  occurs,
  primitive,
  replaceScope,
  typeKey,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const starClass: DefinitionType = defineType({
  id: DefinitionTypeId.StarClass,
  source: keyedBlocks("common/star_classes", true, { rootKeyFilter: { mode: "exclude", values: ["random_list"] } }),
  variants: [],
  localisation: [definitionLocalisation("Name", "", true), definitionLocalisation("Description", "_desc", false)],
  modifiers: [],
  entries: [
    field("class", primitive("scalar"), occurs.one),
    field("icon", primitive("scalar"), occurs.optional),
    field("icon_scale", primitive("number"), occurs.optional),
    field(
      "planet",
      block([field("key", typeRef("planet_class"), occurs.one), field("class", primitive("scalar"), occurs.optional)]),
      occurs.any,
    ),
    field("spawn_odds", primitive("number"), occurs.optional),
    field(
      "num_planets",
      block([field("min", primitive("integer"), occurs.one), field("max", primitive("integer"), occurs.one)]),
      occurs.optional,
    ),
    field(typeKey("planet_class"), block([field("spawn_odds", primitive("number"), occurs.one)]), occurs.any),
    field("is_environmental_hazard", primitive("boolean"), occurs.optional),
    field("can_be_crisis_terraformed", primitive("boolean"), occurs.optional),
    field("crisis_star_class", typeRef("star_class"), occurs.optional),
    field("modifier", block([modifierEntries()]), occurs.optional, { scope: replaceScope({ root: ScopeId.Ship }) }),
  ],
});
