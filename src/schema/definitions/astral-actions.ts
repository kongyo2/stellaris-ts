// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId } from "../catalog.js";

import {
  block,
  defineType,
  effectEntries,
  enumRef,
  field,
  keyedBlocks,
  modifierRuleEntries,
  occurs,
  primitive,
  scriptValue,
  triggerEntries,
  typeKey,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const astralActions: DefinitionType = defineType({
  id: DefinitionTypeId.AstralActions,
  source: keyedBlocks("common/astral_actions"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("sound", typeRef("sound"), occurs.optional),
    field("sound", typeRef("sound_effect"), occurs.optional),
    field("unlock_threshold", primitive("integer"), occurs.optional),
    field("usages", primitive("integer"), occurs.optional),
    field("custom_usage_tooltip", primitive("localisation"), occurs.optional),
    field("upgrade", enumRef(EnumId.AstralActionsUpgrade), occurs.optional),
    field(
      "activation_cost",
      block([
        field(
          "cost",
          block([
            field("trigger", block([triggerEntries()]), occurs.optional),
            field("astral_threads", primitive("integer"), occurs.optional),
            field(typeKey("resource"), primitive("integer"), occurs.optional),
            field("multiplier", scriptValue("number"), occurs.optional),
          ]),
          occurs.any,
        ),
      ]),
      occurs.optional,
    ),
    field("cooldown", primitive("number"), occurs.optional),
    field("uses_custom_cooldown", primitive("boolean"), occurs.optional),
    field("picture", typeRef("sprite"), occurs.optional),
    field("allow_unlock", block([triggerEntries()]), occurs.optional),
    field("is_exhausted", block([triggerEntries()]), occurs.optional),
    field("potential", block([triggerEntries()]), occurs.optional),
    field("allow", block([triggerEntries()]), occurs.optional),
    field("effect", block([effectEntries()]), occurs.optional),
    field(
      "ai_weight",
      block([field("weight", primitive("number"), occurs.one), modifierRuleEntries()]),
      occurs.optional,
    ),
  ],
});
