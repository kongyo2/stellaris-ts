// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId } from "../catalog.js";

import { defineType, enumRef, field, fieldEquals, keyedBlocks, occurs, primitive } from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const agreementTerm: DefinitionType = defineType({
  id: DefinitionTypeId.AgreementTerm,
  source: keyedBlocks("common/agreement_terms"),
  variants: [
    { id: "hidden", when: fieldEquals("hidden", true) },
    { id: "discrete", when: fieldEquals("term_type", "discrete") },
    { id: "discrete_number", when: fieldEquals("term_type", "discrete_number") },
    { id: "specialist_type", when: fieldEquals("term_type", "specialist_type") },
    { id: "resource", when: fieldEquals("term_type", "resource") },
  ],
  localisation: [],
  modifiers: [],
  entries: [
    field("term_type", enumRef(EnumId.AgreementTermType), occurs.one, {
      documentation:
        "This defines how the term will show up in the UI. If it shows up as discrete/separate buttons or if it's a scale.",
    }),
    field("hidden", primitive("boolean"), occurs.optional, {
      documentation: "Whether the term should show up on the agreement negotiation screen (default: no)",
    }),
  ],
});
