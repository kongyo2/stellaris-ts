// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  definitionLocalisation,
  effectEntries,
  field,
  keyedBlocks,
  modifierEntries,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const federationPerk: DefinitionType = defineType({
  id: DefinitionTypeId.FederationPerk,
  source: keyedBlocks("common/federation_perks"),
  variants: [],
  localisation: [definitionLocalisation("Name", "", true), definitionLocalisation("Description", "_desc", true)],
  modifiers: [],
  entries: [
    field("custom_tooltip", primitive("localisation"), occurs.optional),
    field("potential", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Federation }),
    }),
    field(
      "members_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "member_triggered_modifier",
      block([
        field("key", primitive("scalar"), occurs.optional),
        field("potential", block([triggerEntries()]), occurs.optional),
        modifierEntries(),
      ]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "leader_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "leader_triggered_modifier",
      block([
        field("key", primitive("scalar"), occurs.optional),
        field("potential", block([triggerEntries()]), occurs.optional),
        modifierEntries(),
      ]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "envoy_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Federation }) },
    ),
    field(
      "federation_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Federation }) },
    ),
    field(
      "federation_triggered_modifier",
      block([field("potential", block([triggerEntries()]), occurs.optional), modifierEntries()]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.Federation }) },
    ),
    field("on_activate", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Federation }),
    }),
    field("on_deactivate", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Federation }),
    }),
    field("custom_tooltip", primitive("localisation"), occurs.optional),
    field("potential", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Federation }),
    }),
    field(
      "members_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "member_triggered_modifier",
      block([
        field("key", primitive("scalar"), occurs.optional),
        field("potential", block([triggerEntries()]), occurs.optional),
        modifierEntries(),
      ]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "leader_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "leader_triggered_modifier",
      block([
        field("key", primitive("scalar"), occurs.optional),
        field("potential", block([triggerEntries()]), occurs.optional),
        modifierEntries(),
      ]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field(
      "envoy_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Federation }) },
    ),
    field(
      "federation_modifier",
      block([field("description", primitive("localisation"), occurs.optional), modifierEntries()]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Federation }) },
    ),
    field(
      "federation_triggered_modifier",
      block([field("potential", block([triggerEntries()]), occurs.optional), modifierEntries()]),
      occurs.any,
      { scope: replaceScope({ root: ScopeId.Federation }) },
    ),
    field("on_activate", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Federation }),
    }),
    field("on_deactivate", block([effectEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Federation }),
    }),
  ],
});
