// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, EnumId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  enumRef,
  field,
  item,
  keyedBlocks,
  occurs,
  replaceScope,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const zoneSlots: DefinitionType = defineType({
  id: DefinitionTypeId.ZoneSlots,
  source: keyedBlocks("common/zone_slots"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("start", typeRef("zones"), occurs.optional),
    field("start", enumRef(EnumId.ZoneType), occurs.optional),
    field(
      "included_zone_sets",
      block([item(enumRef(EnumId.ZoneSet), occurs.any), item(enumRef(EnumId.ZoneSetAdditional), occurs.any)]),
      occurs.optional,
    ),
    field(
      "include",
      block([item(typeRef("zones"), occurs.any), item(enumRef(EnumId.ZoneType), occurs.any)]),
      occurs.optional,
    ),
    field(
      "exclude",
      block([item(typeRef("zones"), occurs.any), item(enumRef(EnumId.ZoneType), occurs.any)]),
      occurs.optional,
    ),
    field("potential", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Planet, from: ScopeId.Planet }),
    }),
    field("unlock", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Planet, from: ScopeId.Planet }),
    }),
    field("start", typeRef("zones"), occurs.optional),
    field("start", enumRef(EnumId.ZoneType), occurs.optional),
    field(
      "included_zone_sets",
      block([item(enumRef(EnumId.ZoneSet), occurs.any), item(enumRef(EnumId.ZoneSetAdditional), occurs.any)]),
      occurs.optional,
    ),
    field(
      "include",
      block([item(typeRef("zones"), occurs.any), item(enumRef(EnumId.ZoneType), occurs.any)]),
      occurs.optional,
    ),
    field(
      "exclude",
      block([item(typeRef("zones"), occurs.any), item(enumRef(EnumId.ZoneType), occurs.any)]),
      occurs.optional,
    ),
    field("potential", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Planet, from: ScopeId.Planet }),
    }),
    field("unlock", block([triggerEntries()]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Planet, from: ScopeId.Planet }),
    }),
  ],
});
