// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  block,
  defineType,
  effectEntries,
  field,
  item,
  keyedBlocks,
  literal,
  modifierRuleEntries,
  occurs,
  primitive,
  replaceScope,
  ruleSetEntries,
  scriptValue,
  triggerEntries,
  typeRef,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const stormType: DefinitionType = defineType({
  id: DefinitionTypeId.StormType,
  source: keyedBlocks("common/storm_types"),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("affect_hp_regen", literal(true), occurs.optional),
    field("affect_armor_regen", literal(true), occurs.optional),
    field("affect_shield_regen", literal(true), occurs.optional),
    field("occlude_system", literal(true), occurs.optional),
    field("spawn_weight", primitive("number"), occurs.optional),
    field(
      "spawn_weight",
      block([
        field("base", primitive("number"), occurs.one),
        field(
          "modifier",
          block([
            field("set", primitive("integer"), occurs.optional),
            field("mult", scriptValue("number"), occurs.optional),
            field("factor", primitive("integer"), occurs.optional),
            triggerEntries(),
          ]),
          occurs.any,
        ),
      ]),
      occurs.optional,
      { scope: replaceScope({ root: ScopeId.Country }) },
    ),
    field("description", primitive("localisation"), occurs.one),
    field("custom_tooltip", primitive("localisation"), occurs.optional),
    ruleSetEntries("triggered_planet_modifier_planet"),
    ruleSetEntries("triggered_fleet_modifier_fleet"),
    ruleSetEntries("triggered_ship_modifier"),
    ruleSetEntries("triggered_system_modifier_starbase"),
    field("on_start", block([effectEntries()]), occurs.one, { scope: replaceScope({ root: ScopeId.CosmicStorm }) }),
    field("on_moved", block([effectEntries()]), occurs.one, { scope: replaceScope({ root: ScopeId.CosmicStorm }) }),
    field("on_finished", block([effectEntries()]), occurs.one, { scope: replaceScope({ root: ScopeId.CosmicStorm }) }),
    field("cosmic_storm_texture_path", primitive("file"), occurs.one),
    field("cosmic_storm_texture_color_path", primitive("file"), occurs.one),
    field("cosmic_storm_event_sprite", typeRef("sprite"), occurs.one),
    field("icon", typeRef("sprite"), occurs.one),
    field("cosmic_storm_texture_lightning_paths", block([item(primitive("file"), occurs.any)]), occurs.one),
    field("cosmic_storm_galaxy_lightning_time", primitive("number"), occurs.one),
    field("cosmic_storm_galaxy_max_opacity", primitive("number"), occurs.one),
    field("show_notification", block([triggerEntries()]), occurs.any, {
      scope: replaceScope({ root: ScopeId.Country }),
    }),
    field("storm_min_radius", block([modifierRuleEntries()]), occurs.optional),
    field("storm_max_radius", block([modifierRuleEntries()]), occurs.optional),
    field("storm_min_steps", block([modifierRuleEntries()]), occurs.optional),
    field("storm_max_steps", block([modifierRuleEntries()]), occurs.optional),
    field("storm_speed", block([modifierRuleEntries()]), occurs.optional),
    field("storm_activation_period_in_months", block([modifierRuleEntries()]), occurs.optional),
    field("storm_monthly_added_devastation", block([modifierRuleEntries()]), occurs.optional),
  ],
});
