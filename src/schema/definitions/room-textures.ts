// Generated by `npm run import:cwt` from cwtools-stellaris-config, then maintained by hand.
// Re-running the importer proposes a diff; it never overwrites hand edits automatically.

import { DefinitionTypeId, ScopeId } from "../catalog.js";

import {
  anyKey,
  block,
  defineType,
  field,
  keyedBlocks,
  occurs,
  primitive,
  replaceScope,
  triggerEntries,
} from "../ir.js";

import type { DefinitionType } from "../ir.js";

export const roomTextures: DefinitionType = defineType({
  id: DefinitionTypeId.RoomTextures,
  source: keyedBlocks("gfx/portraits/asset_selectors", true, { files: ["room_textures.txt"] }),
  variants: [],
  localisation: [],
  modifiers: [],
  entries: [
    field("default", primitive("scalar"), occurs.one),
    field("game_setup", block([field(anyKey(), block([triggerEntries()]), occurs.any)]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Country }),
    }),
    field("ruler", block([field(anyKey(), block([triggerEntries()]), occurs.any)]), occurs.optional, {
      scope: replaceScope({ root: ScopeId.Leader }),
    }),
  ],
});
